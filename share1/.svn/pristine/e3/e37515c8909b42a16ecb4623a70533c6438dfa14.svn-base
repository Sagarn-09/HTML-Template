package com.ecartes.validator;

import org.springframework.stereotype.Component;
import org.springframework.validation.Errors;
import org.springframework.validation.ValidationUtils;
import org.springframework.validation.Validator;

import com.ecartes.bean.RawMaterialInwardBean;

@Component
public class RawMaterialValidator implements Validator {
	

	@Override
	public boolean supports(Class<?> clazz) {
		return RawMaterialInwardBean.class.equals(clazz);
	}

	@Override
	public void validate(Object obj, Errors err) {

		System.out.println("mtrl validator");
		
//		ValidationUtils.rejectIfEmpty(err, "assetUID", "rawMaterialInward.assetUID.empty");
//		ValidationUtils.rejectIfEmpty(err, "assetTagToBeEncoded", "rawMaterialInward.assetTagToBeEncoded.empty");
//		ValidationUtils.rejectIfEmpty(err, "typeOfTags", "rawMaterialInward.typeOfTags.empty");
//		ValidationUtils.rejectIfEmpty(err, "trade", "rawMaterialInward.trade.empty");
		
//		ValidationUtils.rejectIfEmpty(err, "assetCategoryId", "rawMaterialInward.assetCategoryId.empty");
//		ValidationUtils.rejectIfEmpty(err, "itemLocationId", "rawMaterialInward.itemLocationId.empty");
//		ValidationUtils.rejectIfEmpty(err, "spaceLocationId", "rawMaterialInward.spaceLocationId.empty");

		if (!err.hasErrors()) {
			RawMaterialInwardBean rawMaterialInwardBean = (RawMaterialInwardBean) obj;

//			if (rawMaterialInwardBean.getAssetUID() != null
//					&& rawMaterialInwardBean.getAssetUID().length() >= 8) {
//				err.rejectValue("assetUID", "assetUID.filed.length");
//			}
			
//			if (rawMaterialInwardBean.getAssetTagToBeEncoded()!= null
//					&& rawMaterialInwardBean.getAssetTagToBeEncoded().length() >= 90) {
//				err.rejectValue("assetTagToBeEncoded", "assetTagToBeEncoded.filed.length");
//			}
//			
//			if (rawMaterialInwardBean.getTypeOfTags()!= null
//					&& rawMaterialInwardBean.getTypeOfTags().length() >= 28) {
//				err.rejectValue("typeOfTags", "typeOfTags.filed.length");
//			}
//			
//			if (rawMaterialInwardBean.getTrade()!= null
//					&& rawMaterialInwardBean.getTrade().length() >= 15) {
//				err.rejectValue("trade", "trade.filed.length");
//			}
						
		}

	}

}
